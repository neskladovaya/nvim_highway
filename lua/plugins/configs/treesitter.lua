local options = {
  ensure_installed = {
        "lua",
        "cpp",
        "c"
},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
